/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { imagesPostImages } from "../../funcs/imagesPostImages.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostImagesRequestBody$inboundSchema.optional(),
};

export const tool$imagesPostImages: ToolDefinition<typeof args> = {
  name: "images_post-images",
  description: `Create image

Create an image from a prompt.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await imagesPostImages(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
