/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { seriesGetSeries } from "../../funcs/seriesGetSeries.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSeriesRequest$inboundSchema,
};

export const tool$seriesGetSeries: ToolDefinition<typeof args> = {
  name: "series_get-series",
  description: `List series`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await seriesGetSeries(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
