/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { seriesPostSeries } from "../../funcs/seriesPostSeries.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostSeriesRequestBody$inboundSchema.optional(),
};

export const tool$seriesPostSeries: ToolDefinition<typeof args> = {
  name: "series_post-series",
  description: `Create series`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await seriesPostSeries(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
