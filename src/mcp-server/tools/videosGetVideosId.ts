/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { videosGetVideosId } from "../../funcs/videosGetVideosId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetVideosIdRequest$inboundSchema,
};

export const tool$videosGetVideosId: ToolDefinition<typeof args> = {
  name: "videos_get-videos-id",
  description: `Get video`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await videosGetVideosId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
