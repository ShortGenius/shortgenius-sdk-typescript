/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { videosPostVideosDraftsUrl } from "../../funcs/videosPostVideosDraftsUrl.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostVideosDraftsUrlRequestBody$inboundSchema.optional(),
};

export const tool$videosPostVideosDraftsUrl: ToolDefinition<typeof args> = {
  name: "videos_post-videos-drafts-url",
  description: `Draft video from URL

Retrieve the content of a webpage and write a video based on it. ___Only text is currently supported___ -- the AI cannot watch videos. After calling this endpoint, call [Create Video](#tag/videos/POST/videos) with the results to create the video.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await videosPostVideosDraftsUrl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
