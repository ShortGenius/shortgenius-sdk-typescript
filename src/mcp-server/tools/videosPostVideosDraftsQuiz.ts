/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { videosPostVideosDraftsQuiz } from "../../funcs/videosPostVideosDraftsQuiz.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostVideosDraftsQuizRequestBody$inboundSchema.optional(),
};

export const tool$videosPostVideosDraftsQuiz: ToolDefinition<typeof args> = {
  name: "videos_post-videos-drafts-quiz",
  description: `Draft quiz video

Make a quiz on the provided topic. After calling this endpoint, call [Create Video](#tag/videos/POST/videos) with the results to create the video.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await videosPostVideosDraftsQuiz(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
