/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getVoices } from "../../funcs/getVoices.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetVoicesRequest$inboundSchema,
};

export const tool$getVoices: ToolDefinition<typeof args> = {
  name: "get-voices",
  description: `List voices

Gets a list of all voices available for text-to-speech.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getVoices(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
