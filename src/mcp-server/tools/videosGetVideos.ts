/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { videosGetVideos } from "../../funcs/videosGetVideos.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetVideosRequest$inboundSchema,
};

export const tool$videosGetVideos: ToolDefinition<typeof args> = {
  name: "videos_get-videos",
  description: `List videos`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await videosGetVideos(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
