/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { administrationGetCredits } from "../../funcs/administrationGetCredits.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$administrationGetCredits: ToolDefinition = {
  name: "administration_get-credits",
  description: `Get usage

Get usage information.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await administrationGetCredits(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
