/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { imagesGetImagesStyles } from "../../funcs/imagesGetImagesStyles.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$imagesGetImagesStyles: ToolDefinition = {
  name: "images_get-images-styles",
  description: `List image styles

Get all the image styles available for creating images.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await imagesGetImagesStyles(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
