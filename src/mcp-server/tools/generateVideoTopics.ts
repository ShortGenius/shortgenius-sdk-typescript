/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { generateVideoTopics } from "../../funcs/generateVideoTopics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GenerateVideoTopicsRequestBody$inboundSchema,
};

export const tool$generateVideoTopics: ToolDefinition<typeof args> = {
  name: "generate-video-topics",
  description: `Generate video topics

Generate ideas for around 50 videos within a given topic. You can then pass these to the [Create video](#tag/videos/POST/videos) endpoint.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await generateVideoTopics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
