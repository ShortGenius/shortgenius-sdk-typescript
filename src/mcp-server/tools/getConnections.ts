/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getConnections } from "../../funcs/getConnections.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$getConnections: ToolDefinition = {
  name: "get-connections",
  description: `List connections

List all the video publishing destinations you have configured.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await getConnections(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
