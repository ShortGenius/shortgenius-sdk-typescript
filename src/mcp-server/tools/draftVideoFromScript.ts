/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { draftVideoFromScript } from "../../funcs/draftVideoFromScript.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DraftVideoFromScriptRequestBody$inboundSchema,
};

export const tool$draftVideoFromScript: ToolDefinition<typeof args> = {
  name: "draft-video-from-script",
  description: `Draft video from script

Write a video using the provided script. After calling this endpoint, call [Create Video](#tag/videos/POST/videos) with the results to create the video.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await draftVideoFromScript(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
