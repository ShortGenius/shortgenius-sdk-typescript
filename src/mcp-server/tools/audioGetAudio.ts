/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { audioGetAudio } from "../../funcs/audioGetAudio.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAudioRequest$inboundSchema,
};

export const tool$audioGetAudio: ToolDefinition<typeof args> = {
  name: "audio_get-audio",
  description: `List audio

Get all the speech generations you have created.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await audioGetAudio(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
