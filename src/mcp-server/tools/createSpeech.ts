/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { createSpeech } from "../../funcs/createSpeech.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateSpeechRequestBody$inboundSchema,
};

export const tool$createSpeech: ToolDefinition<typeof args> = {
  name: "create-speech",
  description: `Create speech

Generate speech from provided text.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await createSpeech(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
