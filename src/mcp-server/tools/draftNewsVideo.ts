/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { draftNewsVideo } from "../../funcs/draftNewsVideo.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DraftNewsVideoRequestBody$inboundSchema,
};

export const tool$draftNewsVideo: ToolDefinition<typeof args> = {
  name: "draft-news-video",
  description: `Draft news video

Retrieve the latest news on the provided topic, then generate video scenes. After calling this endpoint, call [Create Video](#tag/videos/POST/videos) with the results to create the video.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await draftNewsVideo(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
