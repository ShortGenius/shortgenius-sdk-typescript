/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getAllAudio } from "../../funcs/getAllAudio.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllAudioRequest$inboundSchema,
};

export const tool$getAllAudio: ToolDefinition<typeof args> = {
  name: "get-all-audio",
  description: `List audio

Get all the speech generations you have created.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getAllAudio(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
