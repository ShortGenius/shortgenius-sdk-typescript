/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { audioPostAudioSpeech } from "../../funcs/audioPostAudioSpeech.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostAudioSpeechRequestBody$inboundSchema.optional(),
};

export const tool$audioPostAudioSpeech: ToolDefinition<typeof args> = {
  name: "audio_post-audio-speech",
  description: `Create speech

Generate speech from provided text.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await audioPostAudioSpeech(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
